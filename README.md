
  # Implement in React

  This is a code bundle for Implement in React. The original project is available at https://www.figma.com/design/N12WS3Z9ExKVa0ZIL742OR/Implement-in-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  